import React from 'react';
import logo from '../assets/MESlogo-01.gif';

export const Loader = () => {
    return (
        <div>
            <img src={logo} alt="Loading..." />
        </div>
    );
};
